use crate::{metadata::MetaData, primer::Base};

pub struct Scaffolder {}
impl Scaffolder {
    pub fn add_scaffold(
        &self,
        metadata: MetaData,
        encoded_dna_sequences: Vec<Vec<Base>>,
    ) -> Vec<Vec<Base>> {
        todo!()
    }
}
